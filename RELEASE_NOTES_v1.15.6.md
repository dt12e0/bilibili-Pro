# bilibili Pro v1.15.6

这是当前稳定发布版，适合手动安装到 Chrome 或 Edge。

## 免责声明

- 本发布包仅用于浏览器扩展开发学习、个人研究和个人浏览体验增强。
- 使用者应自行确认使用方式符合 bilibili / 哔哩哔哩用户协议、版权规则以及所在地法律法规。
- 请勿将本插件用于未授权下载、传播、转载、商业使用或规避平台访问限制。
- 本插件不提供、存储或分发任何 bilibili / 哔哩哔哩视频、音频、弹幕、评论或账号数据。
- 下载相关功能仅面向用户在浏览器中已可合法访问的内容，且不保证所有页面、清晰度或媒体线路可用。
- 如相关权利方或平台认为本项目存在不当之处，请联系维护者处理。

## 重点更新

- 修复部分 B 站视频下载时三条媒体线路全部失败的问题。
- 为 `bilivideo.com` / `bilivideo.cn` 媒体 CDN 请求补充必要请求头。
- 保留每条媒体线路自动重试，减少 CDN 或网络波动导致的失败。

## 功能说明

### 下载增强

- 支持当前分 P、全部分 P、指定分 P 范围下载。
- 支持清晰度选择和最高可用清晰度下载。
- 支持合并 DASH 音频和视频为 MP4。
- 支持轻量模式，分别保存音频和视频流。
- 支持备用线路、自动重试、任务暂停、继续、取消、重试。
- 支持复制下载链接和复制诊断信息。
- 支持文件名组合生成：标题、UP、BV、分 P、清晰度、日期。

### 播放控制

- 支持上一帧、下一帧逐帧播放。
- 支持按住按钮连续逐帧播放。
- 支持自定义左右方向键快进秒数。
- 支持音画倒放。

### 页面控制

- 支持原始 B 站排版和插件增强排版。
- 支持首页干净模式、隐藏热搜、换一换历史回退。
- 支持 ESC 关闭图片预览。
- 支持封面/视频信息增强。
- 支持带鱼屏缩小修复。

### 视频总结

- 支持可拖动“本视频重点”面板。
- 支持 Top5、Top10、Top20 灵敏度。
- 点击重点时间点可跳转播放。

### GIF 制作

- 支持时间段 GIF 制作和手动录制 GIF。
- 支持局部裁剪、帧率、尺寸、质量和比例设置。

### 截图增强

- 支持保存当前帧和复制当前帧。
- 支持在浮窗“截图信息”tab 中操作。
- 支持快捷键截图：默认 `C` 保存当前帧，`X` 复制当前帧。
- 适合截取视频画面、封面参考、台词画面和细节帧。
- 截图功能优先从页面 video 元素抽帧；如果当前视频受跨域限制，可能无法导出。

### 快捷键和工具栏

- 支持查看和自定义快捷键。
- 工具栏保留一键后台下载当前页面视频，默认尝试合并 MP4。

## 安装方式

1. 下载 `bilibili-pro-v1.15.6.zip`。
2. 解压 zip。
3. 打开 `chrome://extensions/` 或 `edge://extensions/`。
4. 开启开发者模式。
5. 点击“加载已解压的扩展程序”。
6. 选择解压出来的插件文件夹。

注意：Release 页面里的 `Source code (zip)` 和 `Source code (tar.gz)` 是 GitHub 自动生成的源码包，不是插件安装包。安装插件请下载 `bilibili-pro-v1.15.6.zip`。

## 使用边界

请仅在个人、合法、合规的范围内使用。本项目维护者不鼓励、不支持任何侵犯版权、违反平台协议或违反法律法规的使用方式。
