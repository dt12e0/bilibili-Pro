# bilibili Pro v1.16.1

这是 `1.16` 的稳定性小版本，重点是发布流程、权限说明和测试清单。

## 重点更新

- 新增正式发布打包脚本。
- 新增 release 包内容校验脚本。
- 确保公开安装包排除私有开发文档和工具脚本。
- README 新增权限说明，解释每个浏览器权限的用途。
- 新增发布前测试清单。
- 补充已知限制说明。

## 打包校验

本版本开始，发布安装包通过专用脚本生成并校验。校验会检查 JS 语法、manifest 资源完整性，并阻止私有开发文档进入公开安装包。

## 安装方式

1. 下载 `bilibili-pro-v1.16.1.zip`。
2. 解压 zip。
3. 打开 `chrome://extensions/` 或 `edge://extensions/`。
4. 开启开发者模式。
5. 点击“加载已解压的扩展程序”。
6. 选择解压出来的插件文件夹。

注意：Release 页面里的 `Source code (zip)` 和 `Source code (tar.gz)` 是 GitHub 自动生成的源码包，不是插件安装包。安装插件请下载 `bilibili-pro-v1.16.1.zip`。

## 使用边界

请仅在个人、合法、合规的范围内使用。本项目维护者不鼓励、不支持任何侵犯版权、违反平台协议或违反法律法规的使用方式。
